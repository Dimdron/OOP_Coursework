#ifndef FIGURE_H
#define FIGURE_H
#include <vector>
#include "color.h"
#include "point.h"

class Figure
{
    typedef PointsContainer std::vector<Point>;
private:
    Color color;
    bool visible;
protected:
    PointsContainer points;
    Figure(int, int);
public:
    Color getColor();
    void setColor(Color);

    bool isVisible() const;
    void setVisible(bool value);
};

#endif // FIGURE_H
